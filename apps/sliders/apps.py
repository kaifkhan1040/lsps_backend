from django.apps import AppConfig


class SlidersConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.sliders"
    label = "sliders"
    verbose_name = "Banner Slider & Admission Popup"
